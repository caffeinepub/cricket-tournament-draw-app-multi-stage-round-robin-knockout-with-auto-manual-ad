{
  "frontend/src/App.tsx": "Main application component with AppHeader and routing between setup, schedule, full schedule, knockout, and profile pages based on tournament state, with toast notifications.",
  "frontend/src/features/tournament/types.ts": "TypeScript type definitions for the tournament domain including Team, Group, Stage, Match, advancement configurations, knockout types, and KnockoutWarnings structure; currentView now includes 'profile' option.",
  "frontend/src/features/tournament/useTournamentStore.ts": "Zustand store managing tournament state with persistence; loadTournamentFromBackend now atomically sets all state properties including numberOfTeams, knockoutStages, roundRobinRounds, and stageAdvancementConfigs to ensure the setup page displays loaded tournament data correctly.",
  "frontend/src/features/tournament/generation.ts": "Core tournament generation logic that creates multiple round-robin stages with sequential group naming; ensures Robin Round 2 is populated exclusively from Robin Round 1 runner-ups in deterministic group-index order by collecting winners first, then runner-ups separately.",
  "frontend/src/features/tournament/groupNaming.ts": "Utility function that generates Excel-like alphabetical group names (A-Z, AA-AZ, BA-BZ, etc.) using proper base-26 conversion logic to support unlimited group counts across all robin rounds; exports both generateGroupName and getGroupName (alias) for backward compatibility, with enhanced documentation explaining the sequential naming behavior.",
  "frontend/src/features/tournament/roundRobinScheduler.ts": "Generates all unique match pairings for a round-robin group ensuring every team plays every other team once.",
  "frontend/src/features/tournament/knockoutGenerator.ts": "Creates knockout bracket matches with automatic Winner-vs-Runner-up pairing logic that avoids same-group matchups when possible, followed by rematch-avoidance reseeding; generates winner placeholders with stable upstream fixture references and returns separate warnings for seeding rules and rematch constraints.",
  "frontend/src/features/tournament/validation.ts": "Validation utilities for tournament configuration with corrected formatDestination helper that displays NextStage destinations as \"Robin Round {stageIndex}\" without off-by-one errors; includes English-only user-facing text.",
  "frontend/src/pages/TournamentSetupPage.tsx": "Tournament setup page with step-by-step configuration flow; includes New Tournament, Manage Tournaments, and Save Tournament buttons in the header; fixed validation function calls to match correct signatures (validateRoundConfig, validateAdvancementRuleCompatibility, getQualifiedTeamCount, formatDestination) and AdvancementConfigDialog props (totalStages instead of roundRobinRounds/knockoutStages).",
  "frontend/src/pages/StageSchedulePage.tsx": "Displays all round-robin stages (sorted by stage number) as editable table grids with View Full Schedule, Back, and Reset actions; shows group and team editing capabilities for all rounds.",
  "frontend/src/pages/KnockoutPage.tsx": "Knockout stage page displaying bracket matches grouped by round with fixture codes; includes winner selection controls, displays both seeding rule warnings and reseeding warnings in separate alert callouts, and shows real teams with origin group serial prefix.",
  "frontend/src/components/AppLayout.tsx": "Shared layout wrapper providing consistent container with responsive padding, spacing, and footer with caffeine.ai attribution across all pages; adjusts spacing for mobile screens.",
  "frontend/src/components/MatchDateTimeEditor.tsx": "Reusable component for editing match date and time with inline validation and automatic persistence to tournament store; handles empty string values for date and time parameters.",
  "frontend/src/components/AdvancementConfigDialog.tsx": "Modal dialog for configuring per-stage advancement rules where users select winner and runner-up destinations (either next round-robin stage, specific knockout entry point, or elimination for Robin Round 2 runner-ups) with dropdowns for available options based on tournament structure.",
  "frontend/src/index.css": "Global styles with OKLCH color system using emerald/teal primary colors (avoiding blue/purple), supporting both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the theme with custom colors, Inter font family, and consistent border radius and shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for tournament CRUD operations and user profile management including getCallerUserProfile, setProfilePrivacy, and updateUsername with automatic cache invalidation.",
  "frontend/src/features/tournament/matchCount.ts": "Calculates match count breakdown for all round-robin rounds and knockout stages based on multi-round configuration structure and per-stage advancement configs to determine team flow between stages.",
  "frontend/src/components/Stage2GroupAssignmentPanel.tsx": "Stage flow preview panel displaying how teams advance between round-robin stages and to knockout, with English-only copy explaining that Robin Round 2 is populated exclusively from Robin Round 1 runner-ups in deterministic group order.",
  "frontend/src/components/KnockoutPairingEditor.tsx": "Knockout pairing editor supporting auto/manual mode toggle with fixture codes; in auto mode displays both seeding rule warnings (Winner-vs-Runner-up and same-group violations) and reseeding warnings (rematch conflicts) using Alert components with English-only text.",
  "frontend/src/features/tournament/qualification.ts": "Shared qualification utility that computes qualified teams and team counts for knockout stages by aggregating teams from all round-robin stages based on their per-stage advancement configurations, excluding eliminated runner-ups from qualification counts.",
  "frontend/src/features/tournament/knockoutRounds.ts": "Shared knockout round constants and utilities defining canonical round names (Pre-Quarterfinal, Quarterfinal, Semifinal, Final) with ordering and helper functions for consistent round detection across the app.",
  "frontend/src/components/RoundRobinGroupTable.tsx": "Displays editable responsive grid of round-robin groups with inline-editable group headers, position dropdowns for team reordering, and editable team names; for Stage 2 (Robin Round 2) displays origin group serial numbers with hyphen instead of current group position.",
  "frontend/src/features/tournament/export.ts": "Frontend-only export utility that serializes Stage data (including groups and team names) to a downloadable JSON file via Blob URL without backend calls or authentication.",
  "frontend/src/pages/FullSchedulePage.tsx": "Full schedule page displaying all round-robin and knockout matches with fixture codes, winner selection controls, warnings display, and a save button at the bottom that triggers Internet Identity login if needed before opening the SaveTournamentDialog.",
  "frontend/src/features/tournament/groupReorder.ts": "Pure helper function that reorders teams within a group by moving a team to a target position, deterministically resolving conflicts by shifting other teams to maintain unique 1..N positions.",
  "frontend/src/features/tournament/roundRobinDisplayCode.ts": "Helper utilities to compute and format team display codes for round-robin matches; for Stage 2 (Robin Round 2) uses origin group serial with hyphen (e.g., \"A-2 Team Name\"), otherwise uses current group position.",
  "frontend/src/features/tournament/knockoutFixtureCode.ts": "Utility functions to generate and retrieve knockout fixture codes (PQ1-PQ8, Q1-Q4, S1-S2, F1) based on round name and match index within the round.",
  "frontend/src/features/tournament/knockoutReseeding.ts": "Client-side reseeding logic that respects Winner-vs-Runner-up and same-group avoidance as highest-priority constraints, then minimizes rematches; uses exhaustive search for ≤8 teams and deterministic permutation evaluation for larger counts, returning detailed English-only warnings.",
  "frontend/src/features/tournament/knockoutPlaceholders.ts": "Utilities to detect placeholder teams, normalize legacy placeholder strings (e.g., \"Winner of PQ1\") into canonical serial labels (e.g., \"PQ1\"), and provide safe display labels for knockout slots when winners are not yet decided.",
  "frontend/src/features/tournament/knockoutWinners.ts": "Pure utilities to apply a winner map to knockout matches using stable upstream fixture references (team1Source/team2Source) to always recompute downstream participants even after placeholders are replaced; includes transitive downstream winner clearing when upstream winners change.",
  "frontend/src/components/KnockoutWinnerSelector.tsx": "Reusable winner-selection control for a single knockout match that lets the organizer select Team 1 or Team 2 as winner, calling the store action to persist the winner; only displays when both teams are real (not placeholders).",
  "frontend/src/features/tournament/originGroupSerial.ts": "Helper utilities to resolve a team's originating group serial (e.g., \"A-2\") by finding the earliest stage where the team appears, and format team displays with origin prefix for Robin Round 2 and Pre-Quarter Finals.",
  "frontend/src/features/tournament/serialization.ts": "Serialization utilities that convert frontend tournament state (stages, groups, advancement configs) into backend-compatible format with proper type mapping for StageType, AdvancementType, and KnockoutEntryType using correct field names (stageIndex -> stageIndex, entryPoint -> entryPoint).",
  "frontend/src/features/tournament/deserialization.ts": "Deserialization utilities that convert backend TournamentView data into complete frontend tournament state format, including numberOfTeams, knockoutStages configuration, roundRobinRounds with group counts, and advancement rules correctly mapped using stageIndex and entryPoint fields.",
  "frontend/src/components/SaveTournamentDialog.tsx": "Dialog component for saving tournaments with two modes (new/update); serializes current tournament state and calls backend mutations to persist groups and stages with loading states.",
  "frontend/src/components/TournamentListDialog.tsx": "Dialog displaying all saved tournaments in a clickable table with inline rename editing and delete actions; clicking a row loads that tournament into the store with explicit setCurrentView('setup') call after loadTournamentFromBackend to ensure the setup page displays with all tournament data populated.",
  "frontend/src/components/DeleteTournamentDialog.tsx": "Confirmation dialog for tournament deletion using AlertDialog; displays tournament name, warns about permanent deletion, and calls deleteTournament mutation on confirmation.",
  "frontend/src/components/NewTournamentButton.tsx": "Button component that resets tournament store to initial state; shows confirmation dialog if current tournament is generated to prevent accidental data loss.",
  "frontend/src/components/AppHeader.tsx": "Header navigation component with profile button and login/logout functionality using Internet Identity; triggers login if unauthenticated when accessing profile.",
  "frontend/src/pages/UserProfilePage.tsx": "User profile page displaying username (editable), join date, privacy toggle, tournament statistics, and list of saved tournaments with loading states and authentication checks.",
  "frontend/src/components/UserTournamentCard.tsx": "Tournament card component displaying tournament name, creation date, group/stage counts with clickable load functionality that deserializes and populates full tournament data into the store, then explicitly sets currentView to 'setup' to ensure proper page navigation."
}