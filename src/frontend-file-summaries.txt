{
  "frontend/src/App.tsx": "Main application component that manages view routing between setup, schedule, and knockout pages based on tournament state, with toast notifications.",
  "frontend/src/features/tournament/types.ts": "TypeScript type definitions for the tournament domain with per-stage advancement configuration (StageAdvancementConfig) that routes winners and runner-ups independently to either next round-robin stages or specific knockout entry points, replacing the old global AdvancementRule enum.",
  "frontend/src/features/tournament/useTournamentStore.ts": "Zustand store managing tournament state with persistence; refactored to support per-stage advancement configurations (stageAdvancementConfigs array) with actions to set individual stage routing and generate tournaments based on the new per-stage model.",
  "frontend/src/features/tournament/generation.ts": "Core tournament generation logic that creates multiple round-robin stages based on per-stage advancement configurations, routing teams to next stages or knockout based on each stage's winner/runner-up destination settings.",
  "frontend/src/features/tournament/groupNaming.ts": "Utility function that generates alphabetical group names (A-Z, then AA-AZ, etc.) based on group index; exports both generateGroupName and getGroupName (alias) for backward compatibility.",
  "frontend/src/features/tournament/roundRobinScheduler.ts": "Generates all unique match pairings for a round-robin group ensuring every team plays every other team once.",
  "frontend/src/features/tournament/knockoutGenerator.ts": "Creates knockout bracket matches from qualified teams using per-stage advancement configs; only pre-populates the first enabled knockout round with actual teams and generates subsequent rounds as placeholder matches with TBD teams (no auto-advance simulation).",
  "frontend/src/features/tournament/validation.ts": "Validation utilities for tournament configuration using the shared qualification utility to correctly compute qualified team counts from per-stage advancement configs and validate exact match with first enabled knockout stage requirements (no byes); includes date/time format validators for match scheduling.",
  "frontend/src/pages/TournamentSetupPage.tsx": "Tournament setup page with step-by-step configuration flow including per-stage advancement configuration UI (opens AdvancementConfigDialog for each stage), dynamic validation showing qualified vs required team counts, and stage flow preview panel for multi-stage tournaments.",
  "frontend/src/pages/StageSchedulePage.tsx": "Displays all round-robin stages (labeled as \"Robin Round N\") with groups and matches in collapsible cards with consistent styling.",
  "frontend/src/pages/KnockoutPage.tsx": "Knockout stage page displaying bracket matches with qualified team count computed from per-stage advancement configs, compatibility validation, and safe rendering of TBD placeholder teams in later rounds without date/time editors.",
  "frontend/src/components/AppLayout.tsx": "Shared layout wrapper providing consistent container, spacing, and footer with caffeine.ai attribution across all pages.",
  "frontend/src/components/MatchDateTimeEditor.tsx": "Reusable component for editing match date and time with inline validation using validateDateFormat and validateTimeFormat functions, and automatic persistence to tournament store.",
  "frontend/src/components/AdvancementConfigDialog.tsx": "Modal dialog for configuring per-stage advancement rules where users select winner and runner-up destinations (either next round-robin stage or specific knockout entry point) with dropdowns for available options based on tournament structure.",
  "frontend/src/index.css": "Global styles with OKLCH color system using emerald/teal primary colors (avoiding blue/purple), supporting both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the theme with custom colors, Inter font family, and consistent border radius and shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file for React Query hooks; currently empty as the app uses client-side state management with Zustand.",
  "frontend/src/features/tournament/matchCount.ts": "Calculates match count breakdown for all round-robin rounds and knockout stages based on multi-round configuration structure and per-stage advancement configs to determine team flow between stages.",
  "frontend/src/components/Stage2GroupAssignmentPanel.tsx": "Stage flow preview panel displaying how teams advance between round-robin stages and to knockout, showing winner and runner-up routing for each configured stage with visual flow indicators.",
  "frontend/src/components/KnockoutPairingEditor.tsx": "Knockout pairing editor supporting auto/manual mode toggle for first-round match assignments, with qualified teams pool calculated using the shared per-stage qualification utility and filtering out TBD placeholder teams.",
  "frontend/src/features/tournament/qualification.ts": "Shared qualification utility that computes qualified teams and team counts for knockout stages by aggregating teams from all round-robin stages based on their per-stage advancement configurations (which stages route winners/runner-ups to which knockout entry points)."
}