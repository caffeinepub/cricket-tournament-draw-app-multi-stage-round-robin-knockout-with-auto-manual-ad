{
  "frontend/src/App.tsx": "Main application component that manages view routing between setup, schedule, full schedule, and knockout pages based on tournament state, with toast notifications.",
  "frontend/src/features/tournament/types.ts": "TypeScript type definitions for the tournament domain including the updated currentView type that now supports 'fullSchedule' view in addition to setup, schedule, and knockout views.",
  "frontend/src/features/tournament/useTournamentStore.ts": "Zustand store managing tournament state with persistence; generates Team array from numberOfTeams before calling generateTournament, correctly destructures the returned object containing stages and knockoutMatches, and supports the new 'fullSchedule' view type for navigation between setup, groups, full schedule, and knockout pages.",
  "frontend/src/features/tournament/generation.ts": "Core tournament generation logic that creates multiple round-robin stages with sequential group naming across stages (using cumulative offset), routes teams to next stages or knockout based on per-stage advancement configurations, and returns both stages and knockout matches; calls generateRoundRobinMatches with correct Group parameter and generateKnockoutMatches with correct parameter order.",
  "frontend/src/features/tournament/groupNaming.ts": "Utility functions that generate alphabetical group names (A-Z, then AA-AZ, etc.) based on group index; includes generateGroupNameWithOffset for sequential naming across stages and maintains backward compatibility with getGroupName alias.",
  "frontend/src/features/tournament/roundRobinScheduler.ts": "Generates all unique match pairings for a round-robin group ensuring every team plays every other team once.",
  "frontend/src/features/tournament/knockoutGenerator.ts": "Creates knockout bracket matches from qualified teams using canonical round names, correctly carrying forward only winner placeholders (one per match) between rounds to ensure standard single-elimination match counts (8→4→2→1).",
  "frontend/src/features/tournament/validation.ts": "Validation utilities for tournament configuration using the shared qualification utility to correctly compute qualified team counts from per-stage advancement configs; includes formatDestination helper that displays NextStage destinations with correct robin round numbers (e.g., 'Robin Round 2' for stageIndex=2) and getStageAdvancementLabel for human-readable advancement summaries.",
  "frontend/src/pages/TournamentSetupPage.tsx": "Tournament setup page with step-by-step configuration flow including a preset button that applies the 12-group → 4-group → Pre-Quarterfinals configuration with sequential group naming (A-L then M-P); includes per-stage advancement configuration, regeneration confirmation, and reset confirmation dialog; mobile-responsive with stacked layout on small screens.",
  "frontend/src/pages/StageSchedulePage.tsx": "Displays Robin Round 1 groups as an editable table grid with View Full Schedule (navigates to full schedule page), Back (returns to setup), and Reset (clears all data) actions; mobile-responsive with wrapped action buttons and multi-column group layout.",
  "frontend/src/pages/KnockoutPage.tsx": "Knockout stage page displaying bracket matches grouped by round using canonical round ordering from KNOCKOUT_ROUND_ORDER; includes Back (returns to full schedule) and Reset (clears all data) actions with confirmation; mobile-responsive with stacked layout on small screens.",
  "frontend/src/components/AppLayout.tsx": "Shared layout wrapper providing consistent container with responsive padding, spacing, and footer with caffeine.ai attribution across all pages; adjusts spacing for mobile screens.",
  "frontend/src/components/MatchDateTimeEditor.tsx": "Reusable component for editing match date and time with inline validation and automatic persistence to tournament store; handles empty string values for date and time parameters.",
  "frontend/src/components/AdvancementConfigDialog.tsx": "Modal dialog for configuring per-stage advancement rules where users select winner and runner-up destinations (either next round-robin stage, specific knockout entry point, or elimination for Robin Round 2 runner-ups) with dropdowns for available options based on tournament structure.",
  "frontend/src/index.css": "Global styles with OKLCH color system using emerald/teal primary colors (avoiding blue/purple), supporting both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the theme with custom colors, Inter font family, and consistent border radius and shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file for React Query hooks; currently empty as the app uses client-side state management with Zustand.",
  "frontend/src/features/tournament/matchCount.ts": "Calculates match count breakdown for all round-robin rounds and knockout stages based on multi-round configuration structure and per-stage advancement configs to determine team flow between stages.",
  "frontend/src/components/Stage2GroupAssignmentPanel.tsx": "Stage flow preview panel displaying how teams advance between round-robin stages and to knockout, showing winner and runner-up routing for each configured stage with visual flow indicators; uses formatDestination to display correct robin round numbers for NextStage destinations and special styling for eliminated runner-ups (red badge and X icon).",
  "frontend/src/components/KnockoutPairingEditor.tsx": "Knockout pairing editor supporting auto/manual mode toggle for first-round match assignments, using the shared knockoutRounds utility to reliably detect the first enabled round and filtering out TBD placeholder teams from the qualified pool; calls assignKnockoutFixture with a single KnockoutFixtureAssignment object.",
  "frontend/src/features/tournament/qualification.ts": "Shared qualification utility that computes qualified teams and team counts for knockout stages by aggregating teams from all round-robin stages based on their per-stage advancement configurations, excluding eliminated runner-ups from qualification counts.",
  "frontend/src/features/tournament/knockoutRounds.ts": "Shared knockout round constants and utilities defining canonical round names (Pre-Quarterfinal, Quarterfinal, Semifinal, Final) with ordering and helper functions for consistent round detection across the app.",
  "frontend/src/components/RoundRobinGroupTable.tsx": "Displays an editable responsive grid of Round Robin groups in multiple columns (1 on mobile, 2 on tablets, 3 on desktop), allowing inline editing of team names that persist to the tournament store.",
  "frontend/src/features/tournament/export.ts": "Frontend-only export utility that serializes Stage data (including groups and team names) to a downloadable JSON file via Blob URL without backend calls or authentication.",
  "frontend/src/pages/FullSchedulePage.tsx": "Full schedule page displaying all round-robin matches grouped by stage and group, plus all knockout matches grouped by round; includes Back to Groups and Proceed to Knockout navigation buttons."
}